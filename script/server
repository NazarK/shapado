echo "stopping server"
ps ax | grep 4000 | grep -v grep | awk '{print $1}' | xargs kill
echo "starting thin as demon"
bundle exec thin start -e development -p 4000 --servers 1 -d
#echo "tailing logs, ctrl c to stop showing logs, server will continue to run"
#tail -f log/production.log
